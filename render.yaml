services:
  - type: web
    name: jaina-coinone-monitor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    healthCheckPath: /health
    plan: starter
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
